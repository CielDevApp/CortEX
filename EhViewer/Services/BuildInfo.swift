import Foundation

/// 自動生成: Scripts/update_build_info.sh によりビルド毎に上書きされる
enum BuildInfo {
    static let tag = "2026-04-25 01:18:54 adeb3b7+dirty \"fix(anim): reader close 時の CPU 100% leak 修正 + 同時再生 default=1\""
}
