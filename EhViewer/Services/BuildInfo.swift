import Foundation

/// 自動生成: Scripts/update_build_info.sh によりビルド毎に上書きされる
enum BuildInfo {
    static let tag = "2026-04-26 22:32:17 224c30a+dirty \"fix(external-folder): stale bookmark 自動 refresh で再起動後の NAS 誤未接続を解消\""
}
